model:
  preset: deep_stpp
  hidden_dim: 128
  spatial_dim: 2
  n_marks: 0

training:
  lr: 0.0003
  weight_decay: 1.0e-5
  grad_clip: 1.0
  max_epochs: 100
  batch_size: 128
  early_stopping_patience: 20

trainer:
  accelerator: cpu
  devices: 1
  logger: csv
  log_dir: logs/

data:
  type: hawkes
  n_train: 200
  n_val: 50
  T: 10.0
